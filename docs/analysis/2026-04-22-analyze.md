# Rules Analysis Report - 2026-04-22

## Summary
- 分析対象: 34 ファイル, 221 ルール
- コード化候補: 6 件（高: 4, 中: 2）
- スキップ: 215 件（judgment系 / prohibition(行動指針) / unknown / skills内 / 既にコード化済み）

## 高優先度

### 1. [notion-workflow.md] 日時のタイムゾーン (スコア: 7)
- **現状**: 「Notion MCP で日時プロパティを設定するとき、必ず `+09:00` を付ける」
- **提案**: `notion-add.ts` の日時引数処理に JST 自動付与ガードを追加。時刻を含む ISO 文字列にタイムゾーンがなければ自動で `+09:00` を付与する。スキル内で `notion-update-page` を直接呼ぶケースもあるため、共通ヘルパー `ensureJST(dateStr)` を `scripts/lib/notion.ts` に追加する
- **違反履歴**: なし（ただし厳守マーク付き＝過去に問題があった可能性大）
- **想定実装**: `scripts/lib/notion.ts` に `ensureJST()` 関数追加。`notion-add.ts` で呼び出し
- **スコア内訳**: format-enforce(3) + 厳守(1) + コード例あり(1) + 頻出ルール(2) = 7

### 2. [notion-workflow.md] キャッシュクリア自動化 (スコア: 6)
- **現状**: 「notion-update-page や notion-add.ts で更新した後、notion-list.ts で確認する前に必ず cache-status.ts --clear を実行する」
- **提案**: `notion-add.ts` の処理完了後に自動で `cache-status.ts --clear` を呼ぶ。既に `notion-add.ts` は validate-entry や recipe-gen を自動実行しているので、同様のパターンで組み込める
- **違反履歴**: なし
- **想定実装**: `scripts/notion/notion-add.ts` の末尾に `invalidateNotionCache()` 呼び出し追加（既に import されている）
- **スコア内訳**: pre-check(3) + 厳守(1) + 既存スクリプト参照(1) + 頻出(1) = 6

### 3. [notion-workflow.md] notion-pull dry-run 異常値検出 (スコア: 5)
- **現状**: 「dry-run の出力に 24:xx 以降の時刻、移動時間 120分超、開始時刻が2時間以上早い場合は実行前にユーザー確認」
- **提案**: `notion-pull.ts` の dry-run 出力をパースし、異常値がある場合に exit code 1 で終了させるバリデーション関数を追加。Claude が目視確認する代わりにスクリプトが機械的に検出する
- **違反履歴**: なし
- **想定実装**: `scripts/notion/notion-pull.ts` に `validateDryRunOutput()` 関数追加
- **スコア内訳**: prohibition(2) + 厳守(1) + 機械的検証可能(2) = 5

### 4. [git-workflow.md] gh pr create 失敗時のフォールバック (スコア: 5)
- **現状**: 「gh pr create が "No commits between main and ..." エラーで失敗した場合、即座に gh api で直接 PR を作成する」
- **提案**: PR 作成をラップするスクリプト `scripts/create-pr.ts` を作成。`gh pr create` を試行し、特定エラーで失敗した場合に自動で `gh api` にフォールバック。スキルの `/pr` から呼び出す
- **違反履歴**: なし（ただしこのセッションでも実際に発動した）
- **想定実装**: `scripts/create-pr.ts` 新規作成
- **スコア内訳**: fallback(3) + 厳守(1) + コード例あり(1) = 5

## 中優先度

### 5. [notion-workflow.md] 重複バリデーション (スコア: 4)
- **現状**: 「Notion MCP で直接登録する前に validate-entry.ts を実行する」
- **状態**: **既に部分的にコード化済み**。`notion-add.ts` 経由なら自動チェック。MCP 直接登録時のみルールが必要
- **提案**: 現状維持（MCP 直接登録を減らす方向にスキルを改善する方が効果的）
- **スコア内訳**: pre-check(3) + 厳守(1) + 既存スクリプト参照(1) - 部分コード化済み(-1) = 4

### 6. [notion-workflow.md] select プロパティスキーマ確認 (スコア: 4)
- **現状**: 「notion-update-page で select プロパティを設定する前に DB スキーマを確認する」
- **提案**: `scripts/lib/notion.ts` にスキーマキャッシュ機能を追加。各 DB の select プロパティの選択肢をキャッシュし、不正な値をセットしようとしたらエラーにする
- **想定実装**: `scripts/lib/notion.ts` に `validateSelectValue(dbName, propName, value)` 追加
- **スコア内訳**: pre-check(3) + 厳守(1) = 4

## スキップ

### judgment系（コード化困難）
- [communication.md] 壁打ち対応 — 対話スタイルの指示、コード化不可
- [communication.md] チーム・コーチ主導の依頼 — 行動指針、コード化不可
- [communication.md] 機材スペックの断定 — Web 検索の判断、コード化不可
- [calendar-sync.md] 基本ルール — 複合的な判断基準
- [task-capture.md] ルール — タスク判断は LLM の仕事

### prohibition（行動指針）
- [calendar-sync.md] 睡眠 — スケジューリング方針
- [git-workflow.md] main 直接コミット禁止 — git hook で強制可能だが、worktree フロー全体の指示
- [git-workflow.md] unstaged changes 操作 — git 操作の判断フロー
- [memory.md] Memory 保存ルール — 保存先の方針
- [notion-workflow.md] MCP サーバー名 — 参照情報、コード化不要
- [notion-workflow.md] ページ本文ルール — 運用方針

### 既にコード化済み
- [notion-workflow.md] タイトル正規化 — `normalizeTitle()` で実装済み
- [notion-workflow.md] 重複バリデーション — `validate-entry.ts` + `notion-add.ts` 内部チェックで部分実装済み

### skills/（レポートのみ、書き換え対象外）
- 41件のスキル内ルール — 個別スキルの手順指示のため、スキル自体の改善として別途対応
